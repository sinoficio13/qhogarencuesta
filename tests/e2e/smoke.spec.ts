/**
 * WU-0 e2e smoke spec — placeholder.
 * Real e2e specs are added in WU-3 (auth) and WU-4 (public survey).
 * This file exists so playwright.config.ts can resolve the testDir.
 */
import { test } from '@playwright/test'

test.skip('placeholder — e2e specs added from WU-3 onwards', () => {
  // intentionally empty
})
